# 2026-04-06: AI Monsters UI Overhaul

## Current State Assessment (08:20 UTC)

### Build Status
- cargo build: PASS (server already built)
- npm run build: PASS (Next.js 16.2.1)
- Dev server running on localhost:3000

### What I Found

**Good:**
- globals.css has a solid design system with CSS variables, animations, rarity glows
- DESIGN.md already exists with detailed color palette, typography, component specs
- API routes for AI image/description generation are wired up
- SpacetimeDB hooks (useGame, useCards, useMatches, etc.) all exist and working

**Broken/Ugly/Missing:**
1. **MainNavigation.tsx** - Generic blue/purple gradient, basic tabs, no premium feel
2. **GameLobby.tsx** - Gray bg, gray cards, very prototype-looking
3. **GameBoard.tsx** - Basic grid, placeholder cards with emoji, no game feel
4. **EnhancedCard.tsx** - Basic white card, gradient stats, no rarity glow
5. **CollectionGallery.tsx** - Dark gradient but generic card grid, no atmosphere
6. **Leaderboard.tsx** - White cards on dark bg, basic, no tier feel
7. **DailyCardGenerator.tsx** - Basic white card layout, generic
8. **PackOpening.tsx** - Has animation, but card reveals are basic

### Design Direction
Dark indie-card-game aesthetic. References: Gwent, Legends of Runeterra, Monster Train.

### Implementation Plan
1. globals.css - add atmospheric background gradients, refine design tokens
2. New `GameCard.tsx` - premium card component with proper flip, hover, rarity glow
3. New `GameBoard.tsx` - real card game board with proper layout
4. Overhaul `GameLobby.tsx` - premium dark design with atmospheric bg
5. Overhaul `CollectionGallery.tsx` - premium gallery
6. Overhaul `Leaderboard.tsx` - dark tiered leaderboard
7. Overhaul `MainNavigation.tsx` - premium nav with atmospheric background
8. Wire up image generation fix (remove window.ai references, wire to API route)

### Image Generation Fix
- card-generator.ts doesn't actually call `window.ai` (the comment is misleading)
- API route `/api/generate-card-image` already calls MiniMax directly
- Need to ensure new cards created in SpacetimeDB get AI images via the API

### Starting Implementation
- Creating new premium components first
- Committing incrementally
